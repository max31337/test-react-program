{
  "name": "ip-geo-monorepo",
  "private": true,
  "workspaces": ["web"],
  "scripts": {
    "build": "cd web && npm install && npm run build"
  },
  "engines": { "node": "18.x" },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.23.8"
  }
}